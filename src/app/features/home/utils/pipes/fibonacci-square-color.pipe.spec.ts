import { FibonacciSquareColorPipe } from './fibonacci-square-color.pipe';

describe('FibonacciSquareColorPipe', () => {
  it('create an instance', () => {
    const pipe = new FibonacciSquareColorPipe();
    expect(pipe).toBeTruthy();
  });
});
